# Library — INDEX

<!-- Pointer-first (the anti-mirror rule): Highspot/SharePoint stay canonical. A file
     earns a local copy ONLY
     if you open and MODIFY it (templates, calculators, worksheets); anything merely
     read or sent is a ROW here with a link — never a copy ('modify' = you produce a
     local working derivative; editing the canonical in its own system earns no copy). A
     stale local deck is worse than no local deck, because it gets trusted. Rows follow
     the provenance
     convention: last_verified records when the link was last checked (the intake
     skill's maintain pass re-verifies rows — until it ships, refresh by hand when you
     notice). -->

| Title | Canonical URL | last_verified | why_kept |
|---|---|---|---|
