# Account Notes — {Account Name}

<!-- Managed by the workos-capture skill. Stays at the account root.
     Salesforce deal-state (stage / ACV / close date / next-step) is NEVER stored here —
     it is queried live. This file holds commitments and strategy only. -->

## Open Commitments

<!-- One bullet per open commitment. Format: - [ ] [YYYY-MM-DD] (me/them) {what} — due: {MM/DD or unspecified} — {opp/context}
     The [YYYY-MM-DD] stamp is the CAPTURE date (when the bullet was written), never the
     event date — sync's dedupe seam depends on it. New bullets append as the section's
     LAST line. The exec-review "commitment overdue" hygiene flag reads this section. -->

## Strategy Notes

<!-- Free-form: pursuit strategy, positioning, competitive angle, political landscape,
     what's working / what's stuck. Narrative, not a log. -->
